node_modules/
dist/
.env
.env.*
!.env.example

# drizzle-kit pull regenerates these migration-tooling byproducts every run.
# Day-0 locks #1/#5: Drizzle is query-only, schema.sql is the canonical DDL —
# only the typed query surface (schema.ts / relations.ts) is committed.
src/db/schema/meta/
src/db/schema/*.sql
